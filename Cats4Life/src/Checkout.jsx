import { useState, useEffect } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './Checkout.css'
import { faker } from "@faker-js/faker";


const Checkout = () => {
    // Stores cart items and total price
    const [cartItems, setCartItems] = useState([]);  // cartItems store array of items the user adds to the cart
    const [total, setTotal] = useState(0);           // total will store the total price of all items in the cart
  
 
    );
  };
  
  export default Checkout; 